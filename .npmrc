# IMPORTANT:
# Some dependencies rely on binaries in node_modules/.bin during postinstall.
# Keep bin-links enabled; if your repo is on a filesystem that blocks symlinks,
# use scripts/ziii.sh which runs installs/builds in a shadow workdir under $HOME.
bin-links=true
